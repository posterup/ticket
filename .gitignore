# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env
.env*.local

# vercel
.vercel

# local tooling
.claude/
.idea/

# typescript
*.tsbuildinfo

# prisma generated client
/generated

# impeccable critique snapshots (local design-review cache).
# Narrower than the whole directory: `.impeccable/design.json` is the DESIGN.md
# sidecar — design authority that tooling reads — and belongs in the repo.
/.impeccable/critique/
/.impeccable/live/
.env*
